# 강사용 운영 가이드

첫 시간은 배포 성공을 최우선으로 둡니다. API 키 발급, CORS, 비밀키 노출, RLS는 학생이 가장 많이 막히는 지점이므로 별도 체크리스트로 다룹니다.

## 추천 흐름

1. GitHub Pages 배포
2. HTML/CSS/JS 역할
3. 날씨 API
4. NEIS 급식 API
5. 급식 투표 localStorage
6. 마을버스 API 설정
7. DB 저장으로 확장
